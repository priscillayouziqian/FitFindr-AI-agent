# FitFindr 🛍️

FitFindr is an AI-powered personal shopping and styling agent. It takes a user's natural language request, searches a mock secondhand clothing database, suggests how to style the found item using the user's existing wardrobe, and generates a ready-to-post social media "fit card".

## 🛠️ Tool Inventory

1. **`search_listings`**
   * **Purpose**: Searches the mock database for items matching user criteria, scored by keyword overlap.
   * **Inputs**: 
     * `description` (str): Keywords describing the desired item.
     * `size` (str | None): The requested size.
     * `max_price` (float | None): The maximum price constraint.
   * **Outputs**: A list of listing dictionaries (list[dict]) sorted by relevance.

2. **`suggest_outfit`**
   * **Purpose**: Uses an LLM to generate outfit combinations combining the thrifted item with the user's wardrobe.
   * **Inputs**: 
     * `new_item` (dict): The selected listing dictionary from Step 1.
     * `wardrobe` (dict): The user's current wardrobe data.
   * **Outputs**: A conversational string (str) containing styling advice.

3. **`create_fit_card`**
   * **Purpose**: Generates a catchy, highly creative social media caption based on the outfit suggestion.
   * **Inputs**: 
     * `outfit` (str): The styling advice generated by Tool 2.
     * `new_item` (dict): The selected listing dictionary.
   * **Outputs**: A short, engaging social media caption string (str).

---

## 🧠 Planning Loop & Decision Making

The agent does not blindly execute tools in a fixed sequence. It uses a **context-aware planning loop**:
1. **Halt on Empty Search**: After parsing the query via Regex and calling Tool 1, it checks the results. If no items match, the agent decides to *stop immediately*, sets an error state, and skips all subsequent LLM calls to save time and resources.
2. **Contextual Prompting**: When calling Tool 2, it checks if the user's wardrobe is empty. If it is, the agent alters its prompt strategy to offer *general* styling advice rather than failing or hallucinating nonexistent clothes.

## 📦 State Management

State is managed using a single `session` dictionary that acts as the Single Source of Truth. The data flows sequentially: `session["search_results"]` feeds the top match into `session["selected_item"]`, which is read by Tool 2 to populate `session["outfit_suggestion"]`, which is then read by Tool 3 to create `session["fit_card"]`.

---

## ⚠️ Error Handling & Graceful Degradation

| Failure Mode | Agent Response | Concrete Example from Testing |
|--------------|----------------|-------------------------------|
| **Zero search results** | Halts interaction and returns a helpful error string. | Tested query: `"designer ballgown size XXS under $5"`. Agent successfully caught the `[]` return, skipped LLM tools, and output: *"Sorry, I couldn't find any items matching your filters. Try adjusting your description or price!"* |
| **Empty Wardrobe** | Adjusts LLM prompt to give generic styling advice. | Tested with `get_empty_wardrobe()`. Agent didn't crash; instead, it returned general ideas like: *"Since your wardrobe is currently empty in the system, here are some general ideas..."* |
| **Missing Outfit String** | Returns static error string instead of crashing. | Passed `""` to Tool 3. Returned: *"Unable to create fit card: outfit details missing."* |

---

## 📝 Spec Reflection

Completing the `planning.md` before writing code was a game-changer. Drawing the Mermaid architecture diagram forced me to think about the "early return" branch (what happens if Tool 1 fails) *before* I even wrote the `agent.py` loop. Because I explicitly defined the failure modes on paper, my agent avoided the classic trap of passing `None` values into an LLM and crashing. It made writing the automated `pytest` cases incredibly straightforward since I already knew my expected edge cases.

---

## 🤖 AI Usage

I utilized AI (ChatGPT/Claude) strategically to help implement the code based on my strict specs:

**Instance 1: Tool 1 Implementation & Refinement**
* **Input**: I provided the Tool 1 block from my `planning.md` (inputs, outputs, keyword overlap scoring requirement) to the AI.
* **Output**: The AI generated a functional script that used `.split()` and `intersection()` for keyword scoring.
* **My Override/Change**: The AI initially wrote an exact string match for size (`item_size.lower() == size.lower()`). I realized this was too fragile for thrift sizes (e.g., searching "M" wouldn't match "S/M"). I manually updated the code to use a substring check (`size.lower() in item_size.lower()`) to make it robust.

**Instance 2: Gradio UI Formatting**
* **Input**: I gave the AI my working `run_agent()` code and the starter `app.py` layout, asking it to map the session outputs to the three UI panels.
* **Output**: The AI generated the `handle_query` function, formatting the top listing string with Markdown bolding (`**Title**`).
* **My Override/Change**: I noticed that Gradio's `Textbox` component does not render Markdown, resulting in literal asterisks showing on screen. I manually stripped the Markdown and replaced it with uppercase text and emojis (`f"✨ {item['title'].upper()} ✨"`) for a much cleaner UI presentation.

## The Wardrobe Schema

`data/wardrobe_schema.json` defines the format your agent uses to represent a user's existing wardrobe. It includes:

- `schema`: field definitions for a wardrobe item
- `example_wardrobe`: a sample wardrobe with 10 items you can use for testing
- `empty_wardrobe`: a starting template for a new user

Load an example wardrobe with:
```python
from utils.data_loader import get_example_wardrobe
wardrobe = get_example_wardrobe()
```

## Where to Start

1. **Read `planning.md` and fill it out before writing any code.**
2. Verify the data loads correctly by running `python utils/data_loader.py`.
3. Build and test each tool individually before connecting them through your planning loop.

Your implementation files go in this same directory. There's no required file structure for your agent code — organize it however makes sense for your design.
